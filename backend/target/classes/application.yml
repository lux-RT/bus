server:
  port: 3000

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bus_safety_system?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: "11223311"
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.bus.entity
  configuration:
    map-underscore-to-camel-case: true
